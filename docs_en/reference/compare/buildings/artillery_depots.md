<a id="артиллерийские-депо"></a>
# Artillery Depots

[← Building comparisons](README.md) · [← All comparisons](../README.md) · [← Quick reference](../../README.md)

Artillery Depots produce Cannons, Howitzers, Bombards, Multi-barrelled Cannons, and Frame guns. The available artillery varies by nation.

> **Baseline values** are the most common values in each column: health = 40000, construction time = 245.94 game seconds, price growth = 200%, wood = 100, stone = 1000, gold = 0, population = 0.
> **Bold** in the table below indicates deviations from these values.

| Building | Nation | Health | Construction time, game s | Price growth, % | Wood | Stone | Gold | Population | Produces |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| **Artillery Depot** `algart` | Algeria | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `ausart` | Austria | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `bavart` | Bavaria | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `denart` | Denmark | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `engart` | England | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `fraart` | France | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `hunart` | Hungary | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `netart` | Netherlands | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `pieart` | Piedmont | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `polart` | Poland | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `porart` | Portugal | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `pruart` | Prussia | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `rusart` | Russia | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `saxart` | Saxony | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `scoart` | Scotland | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `spaart` | Spain | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `sweart` | Sweden | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `swiart` | Switzerland | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `turart` | Turkey | 40000 | 245.94 | 200 | **500** | **1200** | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `ukrart` | Ukraine | 40000 | 245.94 | 200 | **4250** | **4400** | **100** | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
| **Artillery Depot** `venart` | Venice | 40000 | 245.94 | 200 | 100 | 1000 | 0 | 0 | Cannon (`cannon`), Frame gun (`framegun`), Howitzer (`howitzer`), Bombard (`mortar`), Multi-barrelled Cannon (`multicannon`) |
