# Как ведут себя стрелковые юниты

[← Как устроена игра](../../README.md)

Глубокий разбор режимов атаки стрелков: удержания позиции
(`standground`), допуска артиллерии к стрельбе по точке
(`bartprepare`) и отхода от близкой цели (`RunAway`); штрафа к
дальности при движении и бонуса к дальности в
покое, переключение оружия по дистанции, бонус с высокой
позиции. Все ссылки на код — в [Источниках](#источники).

> **Связанные документы:** [Как рассчитывается урон](combat_damage_pipeline.md) —
> формула урона; [Выбор цели и атака с движением](target_selection.md) —
> алгоритм выбора цели; [Приказы юнитам](unit_commands.md) —
> очередь приказов; [Как работает артиллерия](artillery_specifics.md)
> — отдельно про артиллерию.

## Кратко

- **Удержание позиции** (`standground`) — флаг отряда «стоять».
  В этом режиме стрелок ищет врагов на полном расстоянии
  `searchradius` (28–45 клеток), без него —
  только на ~`minsearchdist + 0.375` клеток (почти ближний бой).
- **Допуск к стрельбе по точке** (`bartprepare`) — флаг для
  артиллерии, Башен и Портов: при
  получении `attackpoint`-приказа принудительно отключает
  удержание позиции и активирует поиск целей вокруг точки.
- **Отход от близкой цели** (`RunAway`) — стрелок отступает на
  3,5 клетки, если враг вошёл в его «мёртвую зону»
  (`< minsearchdist`). Не работает при удержании позиции.
- **После движения** (`standtime < 0.25`) стрелок теряет
  до 3 клеток к эффективной максимальной дальности (`uniqrnd × 3`); артиллерия —
  до 1.5 клетки.
- **Состояние бездействия** добавляет `addradius` к дальности
  (около 0,6 клетки).
- **Юниты с несколькими видами оружия** автоматически переключают его по
  дистанции: пушка → ядро на дальней / картечь на ближней;
  Мушкетер 18в. → пуля на дальней / штык в упор.
- **Возвышенность**: на холме (`Y > 0`) стрелок видит дальше:
  `searchdist += goHeight × 2`.

---

<a id="1-standground-vs-обычный-режим"></a>
## 1. Удержание позиции (`standground`) и обычный режим

Главный механизм определения видимости цели — это разный
`maxsearchdist` при удержании позиции и в обычном режиме [^1].

При **удержании позиции** (флаг `bstandground = True` плюс приказ — не
`move`):

```
maxsearchdist = MIN(searchradius, GetMaxAttackRadius)
```

То есть полная дальность оружия (~28-45 клеток).

В **обычном** режиме (без удержания позиции или при приказе `move`):

```
maxsearchdist = minsearchdist + 0.375
```

То есть **почти в ближнем бою** — стрелок обнаруживает врага только когда
тот вплотную подошёл.

### 1.1. Что это значит на практике

| Режим | Что происходит |
|---|---|
| Удержание позиции (`standground`) | Мушкетер на холме делает 5–10 выстрелов до ближнего боя. |
| Обычный (`move`/`attack`) | Тот же Мушкетер успевает 1–2 выстрела, потом враг подбегает. |
| Приказ движения | Отключает удержание позиции: `if (order = move)` блокирует ветку. |

**Для обороны обязательно удержание позиции.** Без него стрелковые юниты
выглядят «глухими»: стоят и не реагируют на врага в дальней зоне,
открывают огонь только на 1-2 клетки.

## 2. Допуск артиллерии к стрельбе по точке (`bartprepare`)

`bartprepare = True` — флаг для артиллерии, Башен (`tow`) и
Портов (`port`). Когда такой юнит получает приказ
`attackpoint(trgx, trgz)` (по площади), скрипт принудительно
переключает режимы [^2]:

| Что меняется | Как |
|---|---|
| `bstandground` | принудительно сбрасывается в `False` |
| `bsearchenemy` | принудительно ставится в `True` (активный поиск) |
| Получает приказ | `attackpoint` с задержкой `attackdelay`/`attackmaxdelay` |

То есть артиллерия с приказом `attackpoint` **не стоит на одном
месте**, а активно ищет цели вокруг указанной точки и обстреливает
любого, кто там окажется.

Без `bartprepare` (мобильная картечница, Мушкетер) команда
`attackpoint` работает как `move(x, z)` — без активного поиска.

См. также [Как работает артиллерия](artillery_specifics.md) §3
о приказе `attackpoint` и подготовке выстрела.

## 3. Отход стрелка из мёртвой зоны (`RunAway`)

Если у стрелкового юнита (`minsearchdist > 0`) враг вошёл в
**мёртвую зону** (между `0` и `minsearchdist`, то есть слишком
близко для выстрела), и юнит **не удерживает позицию** — он отступает
на `gc_unit_runawaydist = 3.5` клетки назад [^3].

### 3.1. Условия запуска

Все три должны быть истинными:

1. Юнит **не** удерживает позицию.
2. Враг — в зоне `[0, minsearchdist]`.
3. Стартовое условие (одно из):
   - `standtime = 0` (только что подошёл / завершил выстрел);
   - `standtime > gc_unit_runawaydelay = 1.3` игровой секунды (стоял
     достаточно долго);
   - **Или** игрок-человек на лёгкой или нормальной сложности —
     тогда эта проверка пропускается.

### 3.2. Сложность и поведение

| Кто играет | Сложность | Когда отступает |
|---|---|---|
| Игрок-человек | Лёгкая / Нормальная | При каждом обновлении, пока враг в `minsearchdist` (поблажка для новичков). |
| Игрок-человек | Сложная / Очень сложная / Невозможная | Только при `standtime = 0` или `> 1.3`: в промежутке юнит успевает выстрелить. |
| ИИ | Любая | Так же, как на сложной и более высокой сложности для человека. |

### 3.3. Стратегические выводы

- **Удерживаешь холм** — удержание позиции (`standground`)
  обязательно, иначе стрелки
  разбегутся при сближении.
- **Отступательная тактика** (отход с обстрелом) — **снять**
  удержание позиции; стрелок отбежит на 3,5 клетки, повернётся, выстрелит,
  снова отбежит.
- **Лёгкая кавалерия** догоняет отходящих стрелков: быстрая конница
  (`fasthorse = 96`) втрое быстрее обычного юнита (`default = 32`).

## 4. Штраф к дальности при движении (`standtime`)

`standtime` — счётчик «сколько времени юнит стоит на месте». При
движении сбрасывается в 0; при остановке начинает расти.

Если `standtime < 0.25` игровой секунды, стрелок «трясётся» — теряет в
эффективной максимальной дальности [^4]:

```
if (standtime < 0.25) AND (weapon.kind ≠ cannister):
    if (NOT bArtillery):
        radiusmax −= 3 × uniqrnd            # пехота: до −3 клетки
    else:
        radiusmax −= 3 × uniqrnd × 0.5      # артиллерия: до −1.5 клетки
```

Где `gc_obj_maxattackradiusdisp = 3` (из `dmscript.global`), и
`uniqrnd` ∈ `[0, 1)` — число, зафиксированное при создании каждого
юнита.

### 4.1. Эффект

- **Стрелок в движении не выстрелит на полную дальность** —
  нужно постоять около 0,25 игровой секунды. Это объясняет
  «промахи» по дальним
  целям при подходе.
- **Картечь не штрафуется** (`kind = cannister` — исключение).
- **Артиллерия штрафуется в 2× меньше** — мортира / пушка после
  короткого движения готова стрелять почти на полную.
- Вместе с механизмом отхода (`RunAway`) создаёт последовательность:
  **отход → пауза 0,25 →
  выстрел → отход**.

## 5. Бонус к дальности бездействующего юнита (`addradius`)

Если юнит бездействует (`statestag` содержит
`gc_statetag_move_idle`), он получает бонус к
`weapon.radiusmax` [^5]:

```
rbonus += weapon[i].addradius     # обычно 32 px = ~0.6 клетки
```

Кому даётся: Мушкетерам, Лучникам и Пушкам — у всех
`addradius = 32 px = 0.6 t`. Для слабых стен
(`gc_obj_usage_weakwall`) дополнительно даётся **+0,36 клетки**
к бонусу `rbonus`.

### 5.1. Эффект

Стационарная защита, например гарнизон на холме с удержанием
позиции, стреляет примерно на **0,6 клетки дальше**, чем тот же
отряд в движении. Вместе с бонусом возвышенности (см. §7) и
устранением штрафа `standtime`
получается заметная прибавка к радиусу обороны.

## 6. Переключение нескольких видов оружия по дистанции

Многие юниты имеют **несколько видов оружия** в слотах `weapon[0]`,
`weapon[1]`, ...). Игра автоматически выбирает нужный по
дистанции до цели — каждое оружие имеет
`radiusmin..radiusmax` [^6]. Если враг в близком диапазоне —
выбирается оружие с маленьким `radiusmin`; иначе — дальнее.

Дополнительно учитывается `attmask`: если у цели `mmask`
совпадает с `weapon[i].attmask` (материал брони), это оружие
приоритетнее. Поэтому **огненные стрелы** выбираются для
построек (их `attmask` содержит `gc_obj_material_building`).

### 6.1. Пары оружия

#### Пушка — ядро против картечи

| Слот | Тип | Урон | Пауза | Дальность (пиксели) | Когда |
|---|---|---:|---:|---|---|
| `weapon[0]`, `PPOINTT` | Урон от ядер (`cannonball`) | 1800 | 350 | 550–2160 | дистанция ≥ 550 пикселей (около 10,3 клетки) |
| `weapon[1]`, `PSMPOINTTPUS` | Урон от картечи (`cannister`) | по области | 350 | 0–450 | враг ближе 450 пикселей (около 8,4 клетки) |

Пехота, подошедшая на ~8 клеток к пушке, автоматически попадает
под **картечь** — массовый урон по области. Поэтому «навал пехоты на
пушку» = картечь в упор. **Атаковать пушку лучше растянутой
линией**, чтобы под взрывом было не больше 9 юнитов (см.
[Как рассчитывается урон §5](combat_damage_pipeline.md) об
ограничении суммарного урона).

#### Мушкетер 18в. — пуля против штыка

| Слот | Тип | Урон | Пауза | Дальность (пиксели) | Когда |
|---|---|---:|---:|---|---|
| `weapon[0]` (штык) | Атака пикой (`pike`) | 5–10 (по нации) | **0** (без паузы между ударами) | 35–65 (около 0,66–1,22 клетки) | в упор |
| `weapon[1]`, `SHOTMUSKET` | Сила выстрела (`bullet`) | 16–29 (по нации) | 140–190 | 400–900 (около 7,5–16,9 клетки) | дальше 7,5 клетки |

Мушкетер после выстрела **не беспомощен** в ближнем бою — у него
**штык** с нулевой паузой (`pause = 0`): он бьёт каждый цикл
анимации. Атаковать
перезаряжающихся Мушкетеров кавалерией — значит получить штыковой бой.

Прокачки штыка идут отдельно от прокачек пули
(`bla.musketeer18.1.X` повышает Силу выстрела, `bullet`; штык остаётся
базовый).

#### Лучник — обычная стрела против огненной

| Слот | Тип | Урон | Пауза | Дальность (пиксели) | Разброс | Особенности |
|---|---|---:|---:|---|---:|---|
| `weapon[0]`, `STRELA` | Стрелы (`arrow`) | 15 | 75 | 400–800 | 175 пикселей | основная стрельба |
| `weapon[1]`, `OSTRELA` | Огненные стрелы (`firearrow`) | **150** | 125 | 400–600 | 200 пикселей | `attmask = building + wood + woodwall` |

Огненные стрелы — **оружие лучников против построек**: урон 150
(в 10 раз больше обычного), но скорострельность хуже на 40 %,
точность на 14 % хуже, и **отрядный бонус к урону не
применяется**. Игра автоматически переключает лучника на
`OSTRELA`, когда цель — здание, деревянный объект (`wood`) или палисад.

#### Прочие

- **Янычар** — пуля + сабля.
- **Стрелец** — пищаль + бердыш.
- **Егерь / драгун** — пуля + сабля.
- **Конные стрелки** (Драбант, `drabant`, и Конный стрелец) —
  пуля + сабля
  верхом.

Везде логика одна: близко — оружие ближнего боя, далеко —
стрелковое.

<a id="7-high-ground--бонус-с-возвышенности"></a>
## 7. Бонус с возвышенности

Если стрелковый юнит стоит на возвышенности (`Y > 0`), его
**дальность поиска** растёт пропорционально высоте [^7]:

```
searchdist += goHeight × 2     # только для стрелков: minsearchdist > melee_radius
```

`goHeight` — Y-координата юнита в клетках. Бонус **только к радиусу
обнаружения**, не к самому выстрелу. Но если враг ещё не в зоне
атаки, юнит начинает движение и выстрелит сразу как только цель
войдёт в `radiusmax`. На практике Мушкетеры с холма стреляют по
наступающим **раньше** = больше выстрелов до ближнего боя.

Холмы создаются параметром `relief` при генерации карты
(на карте «Высокогорье» бонус максимален).

### 7.1. Не работает на

- Юнитов ближнего боя (Пикинер на холме никаких преимуществ не
  получает — он в ближнем бою).
- Юнитов с `searchradius = 0` (мортира, барабанщик и т.д.).

## 8. Открытые эмпирические вопросы

1. **Точная формула направления отхода (`RunAway`).** В какую сторону
   отступает стрелок? «От врага по прямой» или какой-то более
   сложный выбор пути. Замерить по снимкам экрана.
2. **Штраф к дальности для картечи** (`cannister`). Запись говорит
   «не штрафуется», но `radiusmin` для картечи обычно нулевой —
   нужно проверить, что `radiusmax` действительно не дрогает.
3. **Возвышенность для Лучников против урона по области.** Если Лучник стоит
   на холме и стреляет огненной стрелой по зданию — учитывается
   ли поправка `goHeight × 2` в обнаружении здания, или только
   при поиске юнитов?

## Источники

[^1]: `data/scripts/lib/unit.script:7259-7286` —
      `_unit_SearchTarget`, расчёт `maxsearchdist`. Условие:
      `if (bstandground AND order ≠ move) then maxsearchdist := MIN(searchradius, GetMaxAttackRadius) else maxsearchdist := minsearchdist + 0.375;`.

[^2]: `data/scripts/lib/player.script:2456-2463` — обработчик
      `bartprepare` при `attackpoint`-приказе:
      принудительный сброс `bstandground := False`,
      `bsearchenemy := True`, отдача приказа
      `attackpoint(trgx, trgz)` с задержкой подготовки.

[^3]: `data/scripts/lib/unit.script:7363-7369` — механизм отхода
      `RunAway`.
      Параметры: `gc_unit_runawaydelay = 1.3` игровой секунды,
      `gc_unit_runawaydist = 3.5` клетки. Условие включения учитывает
      `bstandground`, `standtime`, флаг `bai` (для
      нормально-человеческой поблажки).

[^4]: `data/scripts/lib/unit.script:8011-8023` — штраф `radiusmax`
      при `standtime < 0.25` игровой секунды. Константа
      `gc_obj_maxattackradiusdisp = 3` (`dmscript.global:116`).

[^5]: `data/scripts/lib/unit.script:8026-8028` — бонус
      `addradius` для бездействующего юнита. Поле `weapon.addradius`,
      обычно 32 пикселя = 0,6 клетки. Для
      `gc_obj_usage_weakwall` — дополнительно +0,36 клетки.

[^6]: `data/scripts/lib/unit.script:6376-6451` —
      `_unit_GetWeaponToAttackIndex`. Выбор слота оружия по
      дистанции и `attmask` цели.

[^7]: `data/scripts/lib/unit.script:5469, 7272` — бонус к дальности
      поиска с возвышенности. Применяется только для стрелковых
      юнитов (где `minsearchdist > melee_radius`).
